// Loaded by NPM on: import Fenrir from 'fenrir';

exports.printMsg = function() {
  console.log("No js to include yet. css is in: '~fenrir/vendor/assets/stylesheets/fenrir/[tools, awoken, utilities]'. Or '~fenrir' if you just need the variables and mixins.");
}
